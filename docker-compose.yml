version: "3.8"

services:
  proxy:
    image: "proxy"
    build:
      dockerfile: Dockerfile
      context: ./nginx
      cache_from:
      - "proxy"
    restart: always
    environment:
      CERTBOT_EMAIL: "***REMOVED***@gmail.com"
    ports:
      - "80:80"
      - "443:443"
    networks:
      - ***REMOVED***-test

  nextjs:
    image: "nextjs"
    build:
      dockerfile: Dockerfile
      context: ./
      cache_from:
      - "nextjs"
    restart: unless-stopped
    depends_on:
      - proxy
    ports:
      - "3001:3001"
    networks:
      - ***REMOVED***-test

networks:
  ***REMOVED***-test:
    driver: bridge
